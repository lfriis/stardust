import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	base: {
		flex: 1,
	},
	container: {
		height: '100%',
	},
	textInput: {
		padding: 10,
		height: 50,
		borderColor: '#eee',
	},
});

export default styles;
